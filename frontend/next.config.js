/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'github.com',
      'images.unsplash.com',
      'img.freepik.com',
      'images.pexels.com',
      's.gravatar.com'
    ],
  },
}

module.exports = nextConfig